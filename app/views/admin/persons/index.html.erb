        <nav class="centered breadcrumbs" role="navigation">
            <%= link_to(admin_root_path, class: "crumbContinue") do %><span>Home</span><% end %><%= link_to(admin_persons_path, class: "crumbStop crumbHere") do %><span>Persons</span><% end %>
        </nav>
        <main class="centered content cards">
        <%= render "shared/controls" %> 
        <div class="cardContainer">       
        <!--[if lt IE 8]>
            <p class="browserupgrade">You are using an <strong>outdated</strong> browser. Please <a href="http://browsehappy.com/">upgrade your browser</a> to improve your experience.</p>
        <![endif]-->
        </div>
        <%= render "shared/controls" %>
        </main>
        <section id="templates">
        <%= render "shared/templates/cards_person" %>
        </section>
        <script>
        $(document).ready(function(){
            $().LNAGateway().listPersons(LNA.loadPersonCards, <%= @page %>);
        });
        </script>